# C# 学习计划

## 目标
- 从零开始掌握 C# 语法和 .NET 控制台应用开发
- 做出第一个可运行的练习项目
- 逐步理解面向对象、集合、LINQ、异步编程等核心概念

## 目录
1. 环境准备
2. 基础语法与控制流
3. 面向对象与类设计
4. 集合、LINQ 与文件操作
5. 异步编程与常用库
6. 小项目练习

## 1. 环境准备（第 1 天）
- 安装 .NET SDK
  - 推荐：通过 Homebrew 安装 `brew install --cask dotnet-sdk`
  - 或访问 https://dotnet.microsoft.com 下载 macOS 版 SDK
- 安装 VS Code
- 安装 C# 扩展：`ms-dotnettools.csharp`
- 验证安装：在终端运行 `dotnet --info`

## 2. 基础语法（第 2-5 天）
- 变量与数据类型：`int`, `double`, `string`, `bool`
- 运算符：算术、比较、逻辑
- 控制流：`if`/`else`, `switch`, `for`, `while`, `foreach`
- 方法定义与调用
- 字符串处理与基本格式化

## 3. 面向对象（第 6-10 天）
- 类与对象
- 字段、属性、方法
- 构造函数与析构函数
- 继承、封装、抽象
- 接口与多态
- `static` 关键字与扩展方法

## 4. 集合与数据处理（第 11-15 天）
- 数组与列表：`Array`, `List<T>`
- 字典与集合：`Dictionary<K,V>`, `HashSet<T>`
- LINQ 基本查询：`Where`, `Select`, `OrderBy`, `FirstOrDefault`
- 异常处理：`try`, `catch`, `finally`, `throw`
- 文件读写：`File.ReadAllText`, `File.WriteAllText`, `StreamReader`, `StreamWriter`

## 5. 异步与常用库（第 16-20 天）
- `async` / `await`
- `Task` 和异步方法
- 使用 `HttpClient` 进行简单网络请求
- JSON 处理：`System.Text.Json`
- 了解 NuGet 包管理与项目引用

## 6. 小项目练习（第 21-30 天）
- 练习项目 1：控制台计算器
- 练习项目 2：文本文件日程管理器
- 练习项目 3：简单数据查询与统计（读取 CSV 或 JSON）

## 练习建议
- 每天写一个小功能，保持持续输出
- 遇到问题先自己调试，再查文档
- 多阅读官方文档与示例代码

## 推荐资源
- 官方文档：https://learn.microsoft.com/dotnet/csharp/
- C# 教程："C# Guide"、"Getting Started with C#"
- 中文参考：`https://learn.microsoft.com/zh-cn/dotnet/csharp/`

## 如何运行这个项目
1. 进入目录：`cd ~/CSharpLearning`
2. 执行：`dotnet build`
3. 运行：`dotnet run`

> 注意：当前系统中尚未检测到 `dotnet` 命令。如果你希望，我也可以继续帮你写一份详细的安装和入门操作说明。

## 10 天渐进学习计划（零基础友好）

这个计划每一天都紧密衔接，避免跳跃。你可以按天完成，并在学习时把每个小示例写入 `Program.cs` 或新建类文件。

- Day 1：环境准备与第一个控制台程序
  - 安装 .NET SDK 和 VS Code
  - 运行 `dotnet --info`
  - 执行 `dotnet run`，理解 `Main()` 方法和 `Console.WriteLine()`
  - 练习：打印自己的名字、数值表达式、字符串拼接
- Day 2：变量与类型
  - 变量声明：`int`, `double`, `string`, `bool`
  - 常量 `const`
  - 类型转换：`int.Parse()`, `Convert.ToInt32()`, `$"..."`
  - 练习：计算两个数的和、格式化输出
- Day 3：条件语句与分支
  - `if` / `else if` / `else`
  - `switch` 语句
  - 练习：判断奇偶、分数等级、简单登录验证
- Day 4：循环与数组
  - `for`, `while`, `foreach`
  - 数组 `int[]`, `string[]`
  - 练习：计算数组总和、查找最大值、遍历字符串数组
- Day 5：方法与代码复用
  - 方法定义、参数、返回值
  - `void` 方法和方法重载
  - 练习：写 `Add(a, b)`, `IsPrime(n)`, `PrintList()`
- Day 6：面向对象基础
  - 定义类、字段、属性、构造函数
  - 创建对象 `new TaskItem()`
  - 练习：实现 `TaskItem` 类，创建并打印任务列表
- Day 7：类行为与访问控制
  - 方法在类中的定义
  - `public`, `private`, `internal`
  - `static` 字段和方法
  - 练习：给 `TaskItem` 添加 `Toggle()`, `MarkDone()`，并在主程序调用
- Day 8：集合与 LINQ
  - `List<T>`, `Dictionary<TKey, TValue>`
  - 基本 LINQ：`Where`, `Select`, `FirstOrDefault`
  - 练习：用 `List<TaskItem>` 过滤并显示未完成任务
- Day 9：异常处理与文件操作
  - `try`, `catch`, `finally`
  - 文件读写：`File.ReadAllText()`, `File.WriteAllText()`
  - 练习：将任务列表保存到文本文件并读取
- Day 10：整合小项目与复习
  - 把前面学到的内容组合成一个简单控制台工具
  - 例如任务管理器、计算器、文本统计器
  - 复习重点：变量、流程、类、集合、方法、异常

### 学习建议

- 每个练习写完后，先手动推演结果，再运行验证。
- 如果某一天内容比较多，可拆成两次完成，但不要跳过基础。
- 你有 Java 基础，C# 的语法和面向对象思想会很快上手。
